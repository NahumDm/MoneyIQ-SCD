# Copy to .env and fill in values. Never commit .env

# Same as auth-service: use mongodb:// (standard) from Atlas if mongodb+srv fails DNS TXT lookup.
MONGODB_URI=mongodb://localhost:27017/expense_db
