# Environment templates (safe to commit)
# Copy each .env.example to .env and fill in real values — .env is gitignored

# Root-level quick reference — see per-service .env.example files:
#   auth-service/.env.example
#   expense-service/.env.example
#   integration-layer/.env.example
#   frontend/.env.example

# Never commit:
#   .env, .env.local, application-local.properties
#   *.pem, *.key, credentials.json, database dumps
